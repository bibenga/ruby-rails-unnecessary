This is a very simple sample Ruby on Rails application:

* app/api – REST API example with authentication and sign-in/sign-out endpoints
* app/graphql – GraphQL API example with authentication and sign-in/sign-out mutations
* app/entrypoints/admin.jsx, app/javascript/admin – React Admin with authentication and Action Cable
